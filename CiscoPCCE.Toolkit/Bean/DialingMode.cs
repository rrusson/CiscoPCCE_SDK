namespace CiscoPCCE.Toolkit.Bean
{
    public enum DialingMode
    {
        INVALID,
        INBOUND,
        PREDICTIVEONLY,
        PREVIEWONLY,
        PROGRESSIVEONLY,
        PREVIEWDIRECTONLY
    }
}